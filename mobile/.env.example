# Replace with your machine's local IP address (run `ipconfig` on Windows or `ifconfig` on Mac/Linux)
# Make sure your mobile device is on the same Wi-Fi network as the backend server

EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api/v1
EXPO_PUBLIC_SOCKET_URL=http://192.168.1.100:3000
